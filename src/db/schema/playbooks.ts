import { boolean, index, pgTable, text, timestamp, uniqueIndex, uuid, varchar } from "drizzle-orm/pg-core";
import { clinics } from "./clinics";
import { objectionCategoryEnum } from "./opportunities";
export const objectionPlaybooks = pgTable("objection_playbooks", { id: uuid("id").defaultRandom().primaryKey(), clinicId: uuid("clinic_id").notNull().references(()=>clinics.id,{onDelete:"restrict"}), category: objectionCategoryEnum("category").notNull(), title: varchar("title",{length:160}).notNull(), objective: text("objective").notNull(), guidelines: text("guidelines").notNull(), suggestedQuestions: text("suggested_questions"), active: boolean("active").default(true).notNull(), createdAt: timestamp("created_at",{withTimezone:true}).defaultNow().notNull(), updatedAt: timestamp("updated_at",{withTimezone:true}).defaultNow().notNull() }, table=>[index("playbooks_clinic_category_idx").on(table.clinicId,table.category),uniqueIndex("playbooks_clinic_title_unique").on(table.clinicId,table.title)]);
